# Maia API 인증 키 (빈 값이면 인증 비활성화)
MAIA_API_KEY=

# Cloudflare Tunnel 토큰
# Cloudflare Zero Trust > Networks > Tunnels > 터널 생성 후 토큰 복사
# 터널 Public Hostname: maia.daybreak.cloud -> http://app:8080
CLOUDFLARE_TUNNEL_TOKEN=
